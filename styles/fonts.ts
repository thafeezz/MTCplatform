import { Lora, Nunito_Sans } from "next/font/google";

export const nunitoSans = Nunito_Sans({ subsets: ["latin"] });
export const lora1 = Lora({
    weight: "500",
    style: "italic",
    subsets: ["latin"],
  });